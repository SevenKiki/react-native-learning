import { phoneReg } from "../../src"

test('11位校验', () => {
    expect(phoneReg('13777851166')).toEqual({
        result: true,
        errMsg: '',
    })
})
test('11位校验', () => {
    expect(phoneReg(' 13777851166 ')).toEqual({
        result: true,
        errMsg: '',
    })
})
test('11位校验', () => {
    expect(phoneReg('137778511667')).toEqual({
        result: false,
        errMsg: '请输入正确的手机号',
    })
})
test('11位校验', () => {
    expect(phoneReg('1377785116')).toEqual({
        result: false,
        errMsg: '请输入正确的手机号',
    })
})
test('规则校验', () => {
    expect(phoneReg('12377785116')).toEqual({
        result: false,
        errMsg: '请输入正确的手机号',
    })
})
test('规则校验', () => {
    expect(phoneReg('12377785116')).toEqual({
        result: false,
        errMsg: '请输入正确的手机号',
    })
})
test('规则校验', () => {
    expect(phoneReg('1237778511x')).toEqual({
        result: false,
        errMsg: '请输入正确的手机号',
    })
})