import React, { Component } from 'react';
import { Image } from 'react-native';
export var LoadingSize;
(function (LoadingSize) {
    LoadingSize[LoadingSize["medium"] = 40] = "medium";
    LoadingSize[LoadingSize["small"] = 25] = "small";
})(LoadingSize || (LoadingSize = {}));
export class KyLoading extends Component {
    constructor(props) {
        super(props);
    }
    render() {
        const { size = LoadingSize.small } = this.props;
        return (<Image style={{
            width: size,
            height: size,
        }} resizeMode={'cover'} source={require('../../../assets/loading.gif')}/>);
    }
}
