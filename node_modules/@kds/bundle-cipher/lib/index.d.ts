export declare enum EncryptType {
    XOR = 1
}
interface EncryptOptions {
    type?: EncryptType;
    pngPath: string;
    bundleFileName: string;
    output?: {
        pngPath?: string;
        bundleFileName?: string;
    };
    manifest?: {
        filename?: string;
    };
}
interface EncryptResult {
    pngMd5: string;
    md5: string;
    pngSize: number;
    bundleSize: number;
}
export declare const encrypt: (options: EncryptOptions) => EncryptResult;
export {};
