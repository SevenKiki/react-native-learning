import { Platform } from 'react-native';
import { loadLocalRawResourceDefault, loadLocalRawResourceAndroid } from './common';

export const loadLocalRawResource =
    Platform.OS !== 'android'
        ? loadLocalRawResourceDefault
        : loadLocalRawResourceAndroid;

export default loadLocalRawResource;