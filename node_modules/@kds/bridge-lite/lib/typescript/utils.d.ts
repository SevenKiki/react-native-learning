export declare const delay: (timeout: number) => Promise<void>;
export declare const dealRejectErrorInfo: (err: {
    code: any;
    message: string;
    error_msg?: string;
    userInfo?: {
        error_msg: string;
    };
}) => any;
export declare const normalParams: (params: any) => string;
export declare const normalNamespace: (name: string) => string;
export declare function generateUUID(): string;
export declare type Version = string | number;
export declare enum VersionIs {
    LessThan = -1,
    EqualTo = 0,
    GreaterThan = 1
}
/**
 * Compare two versions quickly.
 * @param current Is this version greater, equal to, or less than the other?
 * @param other The version to compare against the current version
 * @return 1 if current is greater than other, 0 if they are equal or equivalent, and -1 if current is less than other
 */
export declare function versionCompare(current: Version, other: Version): VersionIs;
export declare function getAppVersion(): string;
