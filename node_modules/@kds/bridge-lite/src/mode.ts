export type Mode = 'development' | 'production';

let currentMode: Mode = 'production';

export const getCurrentMode = () => currentMode;

export const setCurrentMode = (mode: Mode) => {
    currentMode = mode;
};
