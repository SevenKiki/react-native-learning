import { ELiveSocketMessageType } from './messageType';
export declare type TSocketMessageCallback = (data: Object) => void;
/**
 * 订阅长链接信令消息 </br>
 * 支持版本: 9.10.40+ </br>
 * @param msgType   要订阅的信令类型
 * @param callback  接收信令消息的回调
 *
 * @returns 成功则返回 subscribeId（订阅ID），请在不再需要信令数据时调用 {@link unsubscribeSocketMessage} 方法进行反注册，否则会引起泄漏。
 * 一般可以在 component 销毁时注销该订阅。
 */
export declare function subscribeSocketMessage(msgType: ELiveSocketMessageType, callback: TSocketMessageCallback): Promise<string>;
/**
 * 取消订阅长链接信令 </br>
 * 支持版本: 9.10.40+ </br>
 * @param subscribeId   订阅ID
 */
export declare function unsubscribeSocketMessage(subscribeId: string): Promise<void>;
