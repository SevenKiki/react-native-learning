import { liveRnBridgeExecute } from '../utils/liveRnBridgeExecute';
/**
 * 直播路由跳转 </br>
 * 支持版本: 9.0.20+ </br>
 * 直播路由表 主播端、观众端的namespace使用 KwaiLive，开播页使用 Kwai </br>
 * @Deprecated 该 API 已废弃，请使用:liveRouter
 */
export function dispatchLiveRouter(params) {
    return liveRnBridgeExecute('dispatchLiveRouter', { param: params });
}
/**
 * 直播路由跳转 </br>
 * @param params
 */
export function liveRouter(params) {
    return liveRnBridgeExecute('liveRouter', params);
}
/**
 * 回退当前页面，具体依赖于容器实现 </br>
 * 当前通过路由打开的KRN半屏或弹框没有实现该方法，只在某些嵌入的KRN容器里支持该方法。<br>
 * 支持版本: 10.1.10+ </br>
 */
export function popPage() {
    return liveRnBridgeExecute('popPage', {});
}
/**
 * 直播间全屏页曝光埋点接口
 * 支持版本: 10.3.30+ </br>
 */
export function logLivePageEnter() {
    return liveRnBridgeExecute('logLivePageEnter', {});
}
/**
 * 异步获取魔表特效资源路径
 * 支持版本: 10.6.10+ </br>
 * @param params
 */
export function getMaterialResourcePath(params) {
    return liveRnBridgeExecute('getMaterialResourcePath', params);
}
