import { logError } from './utils/logger';
/**
 * 初始化参数
 * @param props
 */
export function init(props) {
    // @ts-ignore
    // eslint-disable-next-line no-underscore-dangle
    global._krn_live_bridge_init_global_data_ = props;
}
export function getInitData() {
    // @ts-ignore
    // eslint-disable-next-line no-underscore-dangle
    if (!global._krn_live_bridge_init_global_data_) {
        logError('', '', '确保使用 live-bridge 前已用 bridge.init(props) 初始化; 或检查控制台是否有其他报错信息');
    }
    // @ts-ignore
    // eslint-disable-next-line no-underscore-dangle
    return global._krn_live_bridge_init_global_data_ || {};
}
