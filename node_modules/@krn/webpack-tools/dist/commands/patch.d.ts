import { Command } from '@oclif/core';
export default class Patch extends Command {
    static description: string;
    static examples: string[];
    static args: {
        name: string;
        description: string;
        required: boolean;
    }[];
    sideEffects(name: string, value: string[] | boolean): void;
    run(): Promise<void>;
}
