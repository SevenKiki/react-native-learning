import { Command } from '@oclif/core';
export default class Install extends Command {
    static description: string;
    static examples: string[];
    isUsedI18n(): boolean;
    installBabel(): Promise<void>;
    installWebpack(): Promise<void>;
    installScripts(): Promise<void>;
    installDeps(): Promise<void>;
    getKrnConfig(): Record<string, any> | null;
    installKrnConfig(bundleIds?: string[]): void;
    run(): Promise<void>;
}
