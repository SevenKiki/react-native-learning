import { Command } from '@oclif/command';
export default class CustomCommand extends Command {
    static hidden: boolean;
    version: string;
    run(): Promise<void>;
    runAnalytics({ id, CLIVersion, ...other }: {
        id?: string;
        CLIVersion?: string;
        [key: string]: any;
    }): Promise<void>;
}
