import { flags } from '@oclif/command';
import CustomCommand from './command';
export default class BuildCore extends CustomCommand {
    static description: string;
    static examples: string[];
    static flags: {
        dev: flags.IOptionFlag<string>;
        minify: flags.IOptionFlag<string>;
        verbose: import("@oclif/parser/lib/flags").IBooleanFlag<boolean>;
        entry: flags.IOptionFlag<string>;
        'dependencies-dest': flags.IOptionFlag<string>;
    };
    private getFlags;
    private getBuildBundleParams;
    private runBuildPlatformBundle;
    private runBuildPlatformBundleAndDependencies;
    run(): Promise<void>;
}
