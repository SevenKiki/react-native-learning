import CustomCommand from './command';
export default class Doctor extends CustomCommand {
    static hidden: boolean;
    static description: string;
    static examples: string[];
    static flags: {
        fix: import("@oclif/parser/lib/flags").IBooleanFlag<boolean>;
    };
    private getFlags;
    run(): Promise<void>;
}
