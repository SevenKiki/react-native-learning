import { LANGUAGE } from './init';
import CustomCommand from './command';
export declare const createBundleTemplate: (bundleId: string) => string;
export declare const createBundle: ({ cwd, bundleDir, bundleId, language, }: {
    cwd: string;
    bundleDir?: string | undefined;
    bundleId: string;
    language: LANGUAGE;
}) => Promise<{
    entryFile: string;
}>;
export default class Create extends CustomCommand {
    static hidden: boolean;
    static description: string;
    static args: {
        name: string;
    }[];
    private getArgs;
    private inputBundleId;
    private inputBundleDir;
    run(): Promise<void>;
}
