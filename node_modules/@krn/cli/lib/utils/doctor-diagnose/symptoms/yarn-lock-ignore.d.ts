import { Symptom } from './base';
export declare class YarnLockIgnore extends Symptom {
    desc: string;
    advice: string;
    level: 'warn' | 'error';
    check(): Promise<boolean>;
    fix(): Promise<boolean>;
}
declare const yarnLockIgnore: YarnLockIgnore;
export default yarnLockIgnore;
