import { Symptom } from './base';
export declare class YarnLock extends Symptom {
    desc: string;
    advice: string;
    level: 'warn' | 'error';
    check(): Promise<boolean>;
    fix(): Promise<boolean>;
}
declare const yarnLock: YarnLock;
export default yarnLock;
