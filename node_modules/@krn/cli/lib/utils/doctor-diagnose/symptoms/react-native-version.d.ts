import { Symptom } from './base';
export declare class ReactNativeVersion extends Symptom {
    desc: string;
    advice: string;
    level: 'warn' | 'error';
    check(): Promise<boolean>;
    fix(): Promise<boolean>;
}
declare const reactNativeVersion: ReactNativeVersion;
export default reactNativeVersion;
