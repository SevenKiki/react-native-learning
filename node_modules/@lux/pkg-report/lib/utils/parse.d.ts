import { IPackage, IParseItemInfo } from '../interface';
/**
 * 从 package.json 中生成 lux 的组件采集信息
 * @param packageJson
 * @returns
 */
export declare const parsePackageJsonComponents: (packageJson: IPackage) => IParseItemInfo[];
