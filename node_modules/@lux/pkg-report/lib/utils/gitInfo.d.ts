import { IGitInfo } from '../interface';
/**
 * 获取当前用户 git 信息
 * @returns
 */
export declare const getGitInfo: () => IGitInfo;
/**
 * 获取用户当前应用目录下的 git 信息
 * @returns
 */
export declare const getLocalGitRepoInfo: () => any;
/**
 * 解析git仓库名
 * 区分ssh & https两种格式
 * @param url - git@git.corp.kuaishou.com:mfe/platform/lux-cli.git
 * @returns mfe/platform/lux-cli
*/
export declare const parseGitRepoName: (url: string) => string;
