/**
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the LICENSE
 * file in the root directory of this source tree.
 */
#include <V8LiteExecutorFactory.h>
#include <fbjni/fbjni.h>
#include <folly/Memory.h>
#include <jni.h>
#include <react/jni/JReactMarker.h>
#include <react/jni/JSLogging.h>
#include <react/jni/JavaScriptExecutorHolder.h>

#include "V8Runtime.h"

namespace facebook {
namespace react {

static void installBindings(jsi::Runtime &runtime) {
  react::Logger androidLogger =
      static_cast<void (*)(const std::string &, unsigned int)>(
          &reactAndroidLoggingHook);
  react::bindNativeLogger(runtime, androidLogger);
}

class V8LiteExecutorHolder
    : public jni::HybridClass<V8LiteExecutorHolder, JavaScriptExecutorHolder> {
 public:
  static constexpr auto kJavaDescriptor =
      "Lcom/facebook/v8/liteexecutor/V8LiteExecutor;";

  static jni::local_ref<jhybriddata> initHybrid(
      jni::alias_ref<jclass>,
      const std::string &timezoneId,
      const std::string &traceFilename,
      jboolean enableStartupTrace) {
    JReactMarker::setLogPerfMarkerIfNeeded();

    return makeCxxInstance(folly::make_unique<V8LiteExecutorFactory>(
        installBindings,
        JSIExecutor::defaultTimeoutInvoker,
        timezoneId,
        traceFilename,
        enableStartupTrace));
  }

  static void startTracing(
      jni::alias_ref<jclass>,
      const std::string &categories) {
    V8Runtime::startTracing(categories);
  }

  static void stopTracing(jni::alias_ref<jclass>) {
    V8Runtime::stopTracing();
  }

  static int getCachedDataVersion(jni::alias_ref<jclass>) {
    return V8Runtime::getCachedDataVersion();
  }

  static int getV8Version(jni::alias_ref<jclass>) {
    return V8Runtime::getV8Version();
  }

  static void registerNatives() {
    registerHybrid({
        makeNativeMethod("initHybrid", V8LiteExecutorHolder::initHybrid),
        makeNativeMethod("jniStartTracing", V8LiteExecutorHolder::startTracing),
        makeNativeMethod("jniStopTracing", V8LiteExecutorHolder::stopTracing),
        makeNativeMethod(
            "jniGetCachedDataVersion", V8LiteExecutorHolder::getCachedDataVersion),
        makeNativeMethod("jniGetV8Version", V8LiteExecutorHolder::getV8Version),
    });
  }

 private:
  friend HybridBase;
  using HybridBase::HybridBase;
};

} // namespace react
} // namespace facebook

JNIEXPORT jint JNICALL JNI_OnLoad(JavaVM *vm, void *reserved) {
  return facebook::jni::initialize(
      vm, [] { facebook::react::V8LiteExecutorHolder::registerNatives(); });
}
