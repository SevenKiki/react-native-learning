import React from 'react';
import { Text, StyleSheet, SafeAreaView } from 'react-native';
import useABTest from '../index';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        width: '100%',
        display: 'flex',
        alignItems: 'center',
    },
    text: {
        backgroundColor: '#aaa',
        color: '#ffffff',
        textAlign: 'center',
        width: '100%',
        height: 50,
        lineHeight: 50,
        marginBottom: 10,
    },
    bottom: {
        display: 'flex',
        flexDirection: 'row',
    },
});

const ABKey = 'highlight_equity_card_word_client';

export default () => {
    const display = useABTest(ABKey);

    return (
        <SafeAreaView style={styles.container}>
            <Text style={styles.text}>key：{ABKey}</Text>
            <Text style={styles.text}>value：{String(display)}</Text>
        </SafeAreaView>
    );
};
