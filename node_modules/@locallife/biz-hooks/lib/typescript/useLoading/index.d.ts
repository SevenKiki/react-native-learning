export declare enum LoadingStatus {
    initial = 0,
    loading = 1,
    success = 2,
    fail = 3
}
/**
 * 根据传入方法状态判断页面状态
 * @param
 * onlyOnce: 是否只能请求成功一次。
 *   --true: 只能请求成功一次。如：【入驻】请求成功后不允许再次请求入驻
 *   --false：允许请求多次。如：页面加载数据失败允许重新请求
 * @example const { status, withLoading, isLoading } = useLoading()
 */
export default function useLoading({ onlyOnce }?: {
    onlyOnce?: boolean;
}): {
    status: LoadingStatus;
    withLoading: (task: () => Promise<any>) => Promise<void>;
    isLoading: boolean;
};
