import { RequestConfig } from './RequestConfig';
import { KSBaseResponse } from './BaseResponseModel';
import { IRequestParam, TParams } from '@locallife/base-request';

export interface InterceptorModel {
    requestParams: IRequestParam;

    requestConfig?: RequestConfig;

    response?: KSBaseResponse;

    reason?: KSBaseResponse;
    extra?: TParams;
}
