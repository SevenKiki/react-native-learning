"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.BaseRequestImpl = void 0;

var _RequestConfig = require("../model/RequestConfig");

var _interceptorChain = require("../interceptorChain");

class BaseRequestImpl {
  // eslint-disable-next-line
  initInnerInterceptors(requestInnerInterceptors, responseInnerInterceptors, config) {}

  getDefaultConfig() {
    return _RequestConfig.DefaultRequestConfig;
  }

  get(param, config) {
    return this.request(param, config);
  }

  post(param, config) {
    return this.request(param, config);
  }

  request(param, config) {
    const requestInnerInterceptors = [];
    const responseInnerInterceptors = [];
    const requestConfig = { ...this.getDefaultConfig(),
      ...config
    };
    this.initInnerInterceptors(requestInnerInterceptors, responseInnerInterceptors, requestConfig);
    return (0, _interceptorChain.start)(requestInnerInterceptors, responseInnerInterceptors, requestConfig, param);
  }

}

exports.BaseRequestImpl = BaseRequestImpl;
//# sourceMappingURL=BaseRequestImpl.js.map