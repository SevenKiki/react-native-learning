import { CommonRequestParams, IRequestParam, KSBaseResponse, RequestConfig } from '@locallife/base-request';
export interface IExposedRequest {
    /**
     * 初始化阶段执行一次即可
     */
    init(commonRequestParams?: CommonRequestParams): any;
    request<T extends KSBaseResponse>(param: IRequestParam, config?: RequestConfig): Promise<T>;
    get<T extends KSBaseResponse>(param: IRequestParam, config?: RequestConfig): Promise<T>;
    post<T extends KSBaseResponse>(param: IRequestParam, config?: RequestConfig): Promise<T>;
}
export declare type RequestInstance = IExposedRequest;
export declare const request: RequestInstance;
