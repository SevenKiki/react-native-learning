import { compareVersion } from './compareVersion';

/**
 * 判断版本1是否大于等于版本2
 * @param version1 版本1
 * @param version2 版本2
 * @returns 版本1是否大于等于版本2
 */
export function greaterThanEqualVersion(version1, version2) {
    return compareVersion(version1, version2) >= 0;
}
