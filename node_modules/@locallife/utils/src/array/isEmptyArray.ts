/**
 * 判断数组是否为空
 * @param array
 */
export function isEmptyArray(array) {
    if (
        array &&
        typeof array === 'object' &&
        array.constructor === Array &&
        array.length > 0
    ) {
        return false;
    }
    return true;
}
