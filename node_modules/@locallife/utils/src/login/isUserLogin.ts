import { NativeModules } from 'react-native';

/**
 * 判断当前用户是否处于登陆状态
 * @returns true：已登录/ false：未登录
 */
export function isUserLogin() {
    return NativeModules.KSUserInfo.getUserInfoSync()?.isLogin;
}
