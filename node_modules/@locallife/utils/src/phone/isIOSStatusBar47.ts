import { checkPortraitSize } from './checkPortraitSize';

/**
 * 判断ios的statusBar高度是否是47
 * (12,12pro,13,13pro,12pro max,13pro max)
 */
export function isIOSStatusBar47() {
    return checkPortraitSize(390, 844) || checkPortraitSize(428, 926);
}
