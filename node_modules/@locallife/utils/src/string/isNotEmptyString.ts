import isString from 'lodash/isString';

/**
 * 判断字符串是否非空
 * @param content 输入的字符串
 * @returns 字符串非空：true / 字符串空：false
 */
export function isNotEmptyString(content: string): boolean {
    return isString(content) && content.length > 0;
}
