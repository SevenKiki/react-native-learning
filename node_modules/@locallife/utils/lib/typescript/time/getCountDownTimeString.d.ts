/**
 * 获取倒计时字符串
 * @param timeStamp 终点时间戳
 * @param initTimeStamp 初始化时间戳
 * @param endContent 倒计时结束后显示的字符串
 * @param endCallback 倒计时为0后做的操作
 * @returns 倒计时的时间字符串
 */
export declare function getCountDownTimeString(timeStamp: number, initTimeStamp: number, endContent?: string, endCallback?: () => void): string;
