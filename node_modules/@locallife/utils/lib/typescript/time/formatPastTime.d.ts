/**
 * 计算过去的某个时间距离今天的天数并格式化：
 * 如果不是同一年，显示 xx年x月x日
 * 如果是同一天，显示今天；
 * 如果距离今天 <= daysLimit 显示 x天前
 * 如果距离今天 > daysLimit 显示 x月x日
 * @param timeStamp 时间戳
 * @param daysLimit 天数限制
 * @returns 格式化后的时间
 */
export declare function formatPastTime(timeStamp: number, daysLimit?: number): string;
