import { Dimensions } from 'react-native';
export function checkPortraitSize(portraitWidth, portraitHeight) {
  const window = Dimensions.get('window');
  const windowRes = window.height === portraitHeight && window.width === portraitWidth || window.width === portraitHeight && window.height === portraitWidth;
  const screen = Dimensions.get('screen');
  const screenRes = screen.height === portraitHeight && screen.width === portraitWidth || screen.width === portraitHeight && screen.height === portraitWidth;
  return windowRes || screenRes;
}
//# sourceMappingURL=checkPortraitSize.js.map