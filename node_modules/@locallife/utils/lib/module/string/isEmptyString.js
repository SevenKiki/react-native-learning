import { isNotEmptyString } from './isNotEmptyString';
/**
 * 判断是否是空字符串
 * @param content 输入的字符串
 * @returns 字符串空：true / 字符串非空：false
 */

export function isEmptyString(content) {
  return !isNotEmptyString(content);
}
//# sourceMappingURL=isEmptyString.js.map