import { NativeModules } from 'react-native';
/**
 * 获取当前app的版本
 * @returns 当前app的版本
 */

export function getCurrentAppVersion() {
  var _NativeModules$KSRCTB;

  const deviceInfo = (NativeModules === null || NativeModules === void 0 ? void 0 : (_NativeModules$KSRCTB = NativeModules.KSRCTBridge) === null || _NativeModules$KSRCTB === void 0 ? void 0 : _NativeModules$KSRCTB.deviceInfo) || {};
  const data = deviceInfo.data || {};
  const {
    appVersion
  } = data || {};
  return appVersion;
}
//# sourceMappingURL=getCurrentAppVersion.js.map