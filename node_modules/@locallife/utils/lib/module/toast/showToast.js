import { NativeModules } from 'react-native';
/**
 * 弹出toast
 * @param text toast对应文案
 */

export function showToast(text) {
  let type = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 'normal';
  let isAddToWindow = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : true;
  NativeModules.Kds.invoke('ui', 'showToast', JSON.stringify({
    type: type,
    text,
    isAddToWindow: isAddToWindow
  })).then(() => {});
}
//# sourceMappingURL=showToast.js.map