"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.checkPhoneSize = checkPhoneSize;

var _reactNative = require("react-native");

/**
 * 判断屏幕宽高、窗口宽高是否是某个值
 * @param size 大小
 * @returns 是/否
 */
function checkPhoneSize(size) {
  const window = _reactNative.Dimensions.get('window');

  const windowRes = window.width == size || window.height == size;

  const screen = _reactNative.Dimensions.get('screen');

  const screenRes = screen.width == size || screen.height == size;
  return windowRes || screenRes;
}
//# sourceMappingURL=checkPhoneSize.js.map