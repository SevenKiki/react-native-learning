"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getIPhoneStatusBarHeight = getIPhoneStatusBarHeight;

var _isIOSStatueBar = require("./isIOSStatueBar59");

var _isIOSStatusBar = require("./isIOSStatusBar47");

var _isIOSStatusBar2 = require("./isIOSStatusBar48");

var _isIOSStatusBar3 = require("./isIOSStatusBar50");

var _isIphoneX = require("./isIphoneX");

/**
 * 获取iPhone的statusBar高度
 * @returns iphone对应的statusBar高度
 */
function getIPhoneStatusBarHeight() {
  if ((0, _isIphoneX.isIphoneX)()) {
    if ((0, _isIOSStatusBar.isIOSStatusBar47)()) {
      return 47;
    }

    if ((0, _isIOSStatusBar2.isIOSStatusBar48)()) {
      return 48;
    }

    if ((0, _isIOSStatusBar3.isIOSStatusBar50)()) {
      return 50;
    }

    if ((0, _isIOSStatueBar.isIOSStatusBar59)()) {
      return 59;
    }

    return 44;
  }

  return 20;
}
//# sourceMappingURL=getIPhoneStatusBarHeight.js.map