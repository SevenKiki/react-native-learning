"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isLowPhone = isLowPhone;

var _reactNative = require("react-native");

var _isString = _interopRequireDefault(require("lodash/isString"));

var _isArray = _interopRequireDefault(require("lodash/isArray"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * 判断是否是低端机
 */
function isLowPhone() {
  let isLowPhoneTag = false;
  const useAgent = _reactNative.NativeModules.Kds.userAgent;

  if ((0, _isString.default)(useAgent)) {
    const arr = useAgent.split(' ');

    if ((0, _isArray.default)(arr)) {
      for (let i = 0; i < arr.length; i++) {
        if ((0, _isString.default)(arr[i]) && arr[i].includes('ISLP') && arr[i].endsWith('1')) {
          isLowPhoneTag = true;
        }
      }
    }
  }

  return isLowPhoneTag;
}
//# sourceMappingURL=isLowPhone.js.map