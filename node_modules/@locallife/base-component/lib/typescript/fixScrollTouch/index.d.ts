import { TouchableOpacityProps } from 'react-native';
import { ElementType } from 'react';
declare type IProps = {
    fixDistance?: number;
    children?: ElementType | any;
} & TouchableOpacityProps;
export declare const FixScrollTouchComponent: (props: IProps) => JSX.Element;
export {};
