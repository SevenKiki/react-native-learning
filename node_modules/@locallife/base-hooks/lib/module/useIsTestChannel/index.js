import { NativeModules } from 'react-native';
export default async function useIsTestChannel() {
  return await NativeModules.Kds.invoke('system', 'getAppInfo', '').then(data => {
    if (data.c === 'TEST' || data.c === 'BETA') {
      return true;
    } else {
      return false;
    }
  }).catch(e => {
    throw e;
  });
}
//# sourceMappingURL=index.js.map