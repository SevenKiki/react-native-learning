import isString from 'lodash/isString';

// 判断数组非空
export function notEmptyArray(content) {
  return Array.isArray(content) && (content === null || content === void 0 ? void 0 : content.length) > 0;
}

// 判断对象为空
export function emptyObject(content) {
  return content === null || content === undefined;
}
function myIsString(content) {
  return isString(content);
}

// 判断字符串非空
export function stringNotEmpty(content) {
  return myIsString(content) && content.length > 0;
}
//# sourceMappingURL=emptyUtils.js.map