// 左侧一排图标右侧一排文案
import React, { useCallback } from 'react';
import { View, StyleSheet, TouchableWithoutFeedback } from 'react-native';
import { CustomIcon } from './CustomIcon';
import { notEmptyArray } from '../../utils/emptyUtils';
import { CustomText } from './CustomText';
const styles = StyleSheet.create({
  container: {
    overflow: 'hidden',
    flexDirection: 'row',
    alignItems: 'center'
  }
});
// 左侧一排图标右侧一排文案
export const SingleLineIconAndText = props => {
  var _data$iconInfos, _data$textInfos;
  const {
    data,
    adaptDark = true
  } = props;
  const onPressCard = useCallback(() => {
    if (props.onPress) {
      props.onPress(props.bizTags);
    }
  }, [props]);
  return data && (notEmptyArray(data.iconInfos) || notEmptyArray(data.textInfos)) ? /*#__PURE__*/React.createElement(TouchableWithoutFeedback, {
    onPress: onPressCard
  }, /*#__PURE__*/React.createElement(View, {
    style: [styles.container]
  }, (_data$iconInfos = data.iconInfos) === null || _data$iconInfos === void 0 ? void 0 : _data$iconInfos.map((item, index) => {
    return /*#__PURE__*/React.createElement(CustomIcon, {
      adaptDark: adaptDark,
      iconModel: item,
      adaptScreen: props.adaptScreen,
      key: index
    });
  }), (_data$textInfos = data.textInfos) === null || _data$textInfos === void 0 ? void 0 : _data$textInfos.map((item, index) => {
    return /*#__PURE__*/React.createElement(CustomText, {
      adaptDark: adaptDark,
      textModel: item,
      adaptScreen: props.adaptScreen,
      key: index
    });
  }))) : null;
};
//# sourceMappingURL=SingleLineIconAndText.js.map