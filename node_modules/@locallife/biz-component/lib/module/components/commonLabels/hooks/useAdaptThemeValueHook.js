import { useThemeValue } from '@kid-ui/krn';
import { useContext } from 'react';
import ThemeContext from '@kid-ui/krn/src/theme/context';
export const useAdaptThemeValueHook = function (kidKey) {
  let adaptDark = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
  const theme = useContext(ThemeContext);
  return useThemeValue(kidKey, adaptDark ? theme : 'light');
};
//# sourceMappingURL=useAdaptThemeValueHook.js.map