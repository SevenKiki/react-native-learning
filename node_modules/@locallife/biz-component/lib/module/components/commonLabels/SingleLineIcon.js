// 一排图标
import React, { useCallback } from 'react';
import { View, StyleSheet, TouchableWithoutFeedback } from 'react-native';
import { CustomIcon } from './CustomIcon';
const styles = StyleSheet.create({
  container: {
    overflow: 'hidden',
    flexDirection: 'row',
    alignItems: 'center'
  }
});
// 一排图标
export const SingleLineIcon = props => {
  const {
    data,
    adaptDark = true
  } = props;
  const onPressCard = useCallback(() => {
    if (props.onPress) {
      props.onPress(props.bizTags);
    }
  }, [props]);
  return data && (data === null || data === void 0 ? void 0 : data.length) > 0 ? /*#__PURE__*/React.createElement(TouchableWithoutFeedback, {
    onPress: onPressCard
  }, /*#__PURE__*/React.createElement(View, {
    style: [styles.container]
  }, data === null || data === void 0 ? void 0 : data.map((item, index) => {
    return /*#__PURE__*/React.createElement(CustomIcon, {
      iconModel: item,
      key: index,
      adaptDark: adaptDark,
      adaptScreen: props.adaptScreen
    });
  }))) : null;
};
//# sourceMappingURL=SingleLineIcon.js.map