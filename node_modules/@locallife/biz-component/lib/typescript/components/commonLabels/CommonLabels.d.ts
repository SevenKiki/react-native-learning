import { ViewStyle } from 'react-native';
import { BizTags } from './Models';
import React from 'react';
interface IProps {
    bizTags: Array<BizTags>;
    maxWidth?: number;
    onPress?: (bizTags?: BizTags) => void;
    adaptScreen?: boolean;
    containerStyle?: ViewStyle;
    autoCut?: boolean;
    autoRatio?: boolean;
    ratio?: number;
    adaptDark?: boolean;
}
export declare const CommonLabels: React.MemoExoticComponent<(props: IProps) => JSX.Element>;
export {};
