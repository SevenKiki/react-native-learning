import { CustomQueryClient } from '@locallife/page';
import { useMemo } from 'react';
export function usePagePreFetch(key, queryFn, options) {
  let infinite = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : false;
  return useMemo(() => {
    if (!key || !queryFn) {
      return null;
    }

    if (infinite) {
      return CustomQueryClient.get().prefetchInfiniteQuery(key, queryFn, { ...options,
        meta: {
          prefetch: 1
        }
      });
    }

    return CustomQueryClient.get().prefetchQuery(key, queryFn, { ...options,
      meta: {
        prefetch: 1
      }
    });
  }, []);
}
//# sourceMappingURL=usePagePreFetch.js.map