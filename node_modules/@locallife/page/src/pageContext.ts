import { createContext } from 'react';

export interface ILCPImageContextProps {
    imageSize: number;
    renderTimestamp: number;
}

type TPageContext = {
    loadedData: boolean;
    noOpt?: boolean;
    lcpImages: ILCPImageContextProps[];
};

export const PageContext = createContext(null as TPageContext);
