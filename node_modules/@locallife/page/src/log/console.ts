export function pageLog(tag: string, message: any) {
    console.log(
        `[@locallife/page][${tag}]:${
            typeof message === 'string' ? message : JSON.stringify(message)
        }`,
    );
}
