import { Base } from './base';
export declare namespace ClientEvent {
    interface ElementPackage extends Base.ElementPackage {
        action?: string;
    }
    interface UrlPackage extends Base.UrlPackage {
        page: string;
    }
    enum ShowType {
        UNKNOWN_TYPE = 0,
        PAGE_AUTO = 10,
        PAGE_CUSTOM = 11,
        ELEMENT = 12
    }
    interface ShowEvent extends Base.ShowEvent {
        type?: ShowType;
        element_package?: ElementPackage;
        content_package?: string;
    }
    enum TaskEventType {
        UNKNOWN_TYPE = 0,
        USER_OPERATION = 1,
        STAY_LENGTH_STAT_EVENT = 2,
        BACKGROUND_TASK_EVENT = 3
    }
    interface TaskEvent extends Base.TaskEvent {
        type: TaskEventType;
        content_package?: string;
    }
    interface CustomEvent {
        custom_stat_event: Base.CustomEvent;
    }
    interface EventPackage {
        show_event?: ShowEvent;
        task_event?: TaskEvent;
        stat_package?: CustomEvent;
    }
}
