/**
 * Event 采集器
 * 用于上报自定义事件和关键动作成功率
 */
import { BaseCollect, RadarCore } from '@ks-radar/radar-util';
import { ActionEndDimension, ActionStartDimension, EventDimension } from './interface';
export declare class RadarEventCollect extends BaseCollect {
    constructor(data: {
        core?: RadarCore;
        sampling?: number;
    });
    parseSeedLog(): void;
    event(dimension: EventDimension, value?: object & {
        duration?: number;
        event_count?: number;
    }): void;
    /**
     * 关键动作成功率
     */
    action: {
        start: (dimension: ActionStartDimension) => void;
        end: (dimension: ActionEndDimension) => void;
    };
    destroy(): void;
    private _checkActionDimension;
}
