import { BaseCollect, RadarCore } from '@ks-radar/radar-util';
interface Options {
    lcp?: boolean;
    cls?: boolean;
    fid?: boolean;
    fcp?: boolean;
}
export declare class RadarChromeMetricsCollect extends BaseCollect {
    private options;
    constructor(data: {
        core: RadarCore;
        sampling?: number;
        options: Options;
    });
    private init;
}
export {};
