/**
 *
 * @param beforeHook 在send时触发，可得到request
 * @param afterHook 在readystatechange 且 readyState = 4 时触发, 可得到request和response
 * @returns
 */
export declare function hookXML(beforeHook?: (request: any) => void, afterHook?: (request: any, response: any) => void): boolean | (() => void);
/**
 * 拦截fetch部分
 * @param beforeHook
 * @param afterHook
 * @returns
 */
export declare function hookFetch(beforeHook?: (request: any) => void, afterHook?: (request: any, response: any) => void): () => void;
export declare function fetchAttach(env: any): {
    register(interceptor: any): () => void;
    clear(): void;
    unRegister(): void;
} | null;
