import type { InvocationParameters, InvocationPromise } from '@yoda/bridge/types';
import type { YodaBridges } from '@yoda/bridge';
export declare type Invoke = <T extends keyof YodaBridges>(...args: InvocationParameters<T>) => InvocationPromise<T>;
export interface YodaInterface {
    invoke: Invoke;
    version?: string;
}
