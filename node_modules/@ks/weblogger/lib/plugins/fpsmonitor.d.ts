/**
 * @file fps.js
 * @author kevin (lanlazy@163.com)
 * @created 2018-08-25
 */
import type { FPSMonitorOptions } from '../types/plugins/types';
import { BasePlugin } from '../types/plugins/base';
export default class FPSMonitor extends BasePlugin {
    static key: string;
    private options;
    private count;
    private fpsTickId;
    private loopId;
    private loopBeginTime;
    private loopEndTime;
    /**
     * 兼容写法，如果在 Weblog 初始化设置 plugins 时，不需要设置 weblog
     * @param weblog
     */
    constructor(options?: FPSMonitorOptions);
    destroy(): void;
    private visibilityChange;
    start(): void;
    fpsTick(): void;
    loop(): void;
    stop(): void;
    calculate(): number;
}
