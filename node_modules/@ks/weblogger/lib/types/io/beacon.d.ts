import type { DataOptions } from './types';
/**
 *
 * @param url 发送地址
 * @param data 发送内容
 * @param forceSendBeacon 是否强制使用 sendBeacon 方式发送埋点。
 */
export default function send({ url, data, forceSendBeacon }: DataOptions): boolean;
