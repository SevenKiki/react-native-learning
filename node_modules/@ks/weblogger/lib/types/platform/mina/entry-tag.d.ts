import type { Weblogger, EntryTagType } from "../../config/types";
export default class EntryTag {
    weblog: Weblogger;
    private _tagQueue;
    private _stashQueue;
    private _identityQueue;
    constructor(weblog: Weblogger);
    /**
     * 获取 entryTags 信息
     */
    getEntryTags(): EntryTagType[];
    /**
     * 设置 entryTag 信息
     * @param entryTag
     */
    setEntryTag(entryTag: EntryTagType): number;
    /**
     * 判断是否能消费
     * @param entryTag
     */
    private shouldConsume;
    private pushState;
    private ifIdentityEmpty;
    private ifIdentityRepeat;
    /**
     * 消费 entryTags 信息
     * @param identity
     */
    consumeEntryTag(identity: string): void;
    private updateCommonGlobalAttr;
    /**
     * 清空 entryTags 信息
     * @param identity
     * @param containSelf
     */
    clearEntryTag(identity: string, containSelf?: boolean): void;
}
