/**
 * @file 对于每个 Plugin 的共性，使用一个抽象类来描述
 */
import type { RadarMonitor, CustomEntry } from './interface';
export declare abstract class RadarPlugin {
    protected radar: RadarMonitor;
    constructor(radar: RadarMonitor);
    abstract key: string;
    abstract created(): void;
    abstract destroy(): void;
    abstract when(perf: PerformanceEntry | CustomEntry | PerformanceEntry[], batch?: boolean): boolean;
}
export declare type RadarPluginConstructor<T> = new (radar: RadarMonitor, options?: T) => any;
