import type { UrlObj, PageTypeKey } from "../config/types";
export default class UrlPackage {
    page: string;
    params?: {
        [key: string]: any;
    };
    identity: string;
    page_type: 1 | 2 | 3 | 0;
    coPage?: boolean;
    category?: string;
    constructor(url: UrlObj, pageType?: PageTypeKey);
    update(page?: string, params?: any): void;
    toJSON(): {
        page: string;
        identity: string;
        page_type: 0 | 1 | 2 | 3;
        params: string;
        category: string | undefined;
    };
    /**
     * 生成页面唯一id
     * 未自定义页面唯一ID生成方法，请自行添加后重新尝试
     * @param page
     */
    generatePageId(page: string): string;
}
