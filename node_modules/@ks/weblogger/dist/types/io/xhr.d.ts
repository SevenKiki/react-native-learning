import type { DataOptions } from './types';
export default function send({ url, data, timeout }: DataOptions, callback?: (error?: any) => void): Promise<{
    error?: string;
    response?: string;
}>;
export declare const get: (url: string) => void;
