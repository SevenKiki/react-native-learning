import type { HybridWeblogInitParams } from "../types/configuration";
declare const _default: (options?: HybridWeblogInitParams[0]) => HybridWeblogInitParams[0];
export default _default;
export declare type LogConfig = HybridWeblogInitParams[0];
export declare type BridgeLogConfig = {
    /**
     * 是否禁止 PV，用于将 H5 作为端内元素上报，不会上报 PV 事件
     */
    disablePV?: boolean;
    /**
     * 自定义事件所属的业务线，如 LIVE、ESHOP 等
     */
    biz?: string;
    /**
     * 是否在 bridge 模式下同时开启 HTTP 上报
     */
    openHttpSender?: boolean;
};
