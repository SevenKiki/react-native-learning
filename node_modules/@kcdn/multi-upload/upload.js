#!/usr/bin/env node
const CmdUpload = require('./lib/cmd-upload');

// 通过命令行方式上传文件
function main() {
  const upload = new CmdUpload();
  upload.start();
}

try {
  main();
} catch (e) {
  // 程序异常，退出进程。研发云流水线终止流程
  console.error(e);
  process.exit(100);
}
