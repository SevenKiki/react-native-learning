import React, { useMemo } from 'react';
import { Svg, Path, G } from './svg';
import type { ViewProps } from 'react-native';
import { IconProps } from './types';

export type CheckboxIconProps = ViewProps & IconProps;

const CheckboxIcon: React.FC<CheckboxIconProps> = (props) => {
    const { color, size, backgroundColor = 'transparent', style: propStyle, ...rest } = props;
    const style = useMemo(() => ({
        color,
        backgroundColor,
    }), [color, backgroundColor]);
    return (
        <Svg style={[style, propStyle]} width={size} viewBox="4 4 12 12" height={size} {...rest}>
            <G>
              <Path fill={style.color} d="m15.63 8.219-6.305 6.305-4.478-4.478L6.26 8.63l3.064 3.064 4.89-4.89 1.414 1.414Z" />
            </G>
        </Svg>
    )
};

export default CheckboxIcon;
