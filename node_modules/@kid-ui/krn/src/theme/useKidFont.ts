import { NativeModules } from 'react-native';

const KIDBridge = NativeModules.KIDBridge;
const fonts = KIDBridge?.getFontConfigs?.() || { };

export const useKidFont = (kidKey: string, _theme?: string) => {
    return fonts[kidKey];
}

export default useKidFont;