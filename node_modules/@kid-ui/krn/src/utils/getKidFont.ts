/**
 * useKidFont非hooks用法
 */

import { NativeModules } from 'react-native';

const { KIDBridge } = NativeModules;

const fonts = KIDBridge?.getFontConfigs?.() || {};

export const getKidFont = (kidKey: string, _theme?: string) => {
  return fonts[kidKey];
}