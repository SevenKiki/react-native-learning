import { useContext } from 'react';
import ThemeContext from './context';
import { safeParse } from '../utils';
import { NativeModules } from 'react-native';
const KIDBridge = NativeModules.KIDBridge;
export const useKidColor = (kidKey, customTheme) => {
    var _a, _b, _c;
    const kidValue = safeParse((_a = KIDBridge === null || KIDBridge === void 0 ? void 0 : KIDBridge.getColorConfig) === null || _a === void 0 ? void 0 : _a.call(KIDBridge, kidKey));
    const theme = useContext(ThemeContext);
    if (!kidValue) {
        return kidValue;
    }
    if (typeof kidValue === 'string') {
        return kidValue;
    }
    return (_c = (_b = kidValue[customTheme]) !== null && _b !== void 0 ? _b : kidValue[theme]) !== null && _c !== void 0 ? _c : kidValue.normal;
};
export default useKidColor;
