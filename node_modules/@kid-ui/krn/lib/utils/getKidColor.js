/**
 * useKidColor非hooks用法
 */
import { NativeModules } from 'react-native';
import { safeParse } from './safeParse';
const { KSAppearance, KIDBridge } = NativeModules;
export const getKidColor = (kidKey, customTheme) => {
    var _a, _b, _c, _d;
    const theme = ((_a = KSAppearance === null || KSAppearance === void 0 ? void 0 : KSAppearance.getColorScheme) === null || _a === void 0 ? void 0 : _a.call(KSAppearance)) || customTheme || 'light';
    const kidValue = safeParse((_b = KIDBridge === null || KIDBridge === void 0 ? void 0 : KIDBridge.getColorConfig) === null || _b === void 0 ? void 0 : _b.call(KIDBridge, kidKey));
    if (!kidValue) {
        return kidValue;
    }
    if (typeof kidValue === 'string') {
        return kidValue;
    }
    return (_d = (_c = kidValue[customTheme]) !== null && _c !== void 0 ? _c : kidValue[theme]) !== null && _d !== void 0 ? _d : kidValue.normal;
};
