import React from 'react';
import { noop } from './utils';
import { ScrollPickerProps } from './scroll-picker';
export interface DatePickerProps extends ScrollPickerProps {
    startYear?: number;
    proportion?: number[];
    numberOfYears?: number;
    date?: string | null | undefined;
    onChange?: Function;
}
export declare class DatePicker extends React.Component<DatePickerProps, any> {
    static defaultProps: {
        startYear: number;
        numberOfYears: number;
        date: string;
        onChange: typeof noop;
        proportion: number[];
    };
    constructor(props: DatePickerProps);
    initialize(props: DatePickerProps): {
        list: number[][];
        value: number[];
    };
    getDateInputByString(param: any): any;
    getDays(year: number, month: number): number[];
    UNSAFE_componentWillReceiveProps(nextProps: DatePickerProps): void;
    getViewList(list: Array<Array<any>>): any[][];
    handleChange: (index1: number, index2: number) => void;
    getDateByIndex(list: Array<Array<any>>, value: Array<any>): string;
    render(): JSX.Element;
}
