/// <reference types="react" />
import { TextInputProps as RNTextInputProps, StyleProp, ViewStyle, TextStyle } from 'react-native';
interface TextInputProps extends Omit<RNTextInputProps, 'onChange' | 'onFocus' | 'onBlur'> {
}
interface TextareaStyleProps {
    containerStyle?: StyleProp<ViewStyle>;
    countStyle?: StyleProp<ViewStyle>;
    countLeftStyle?: StyleProp<TextStyle>;
    countRightStyle?: StyleProp<TextStyle>;
}
interface TextareaProps extends TextInputProps, TextareaStyleProps {
    defaultValue?: string;
    value?: string;
    placeholder?: string;
    disabled?: boolean;
    editable?: boolean;
    rows?: number;
    autoHeight?: boolean;
    count?: number;
    extra?: React.ReactNode;
    renderCount?: (inputCount: number, count: number) => React.ReactNode;
    onChange?: (value: string) => void;
}
export { TextareaProps };
