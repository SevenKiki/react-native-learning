var _a, _b;
import { NativeModules, Platform, PixelRatio } from 'react-native';
const { statusBarHeight: STATUS_BAR_HEIGHT, titleBarHeight } = ((_b = (_a = NativeModules.KSRCTBridge) === null || _a === void 0 ? void 0 : _a.deviceInfo) === null || _b === void 0 ? void 0 : _b.data) || {};
const pr = Platform.OS === 'android'
    ? (PixelRatio.get() || 1)
    : 1;
export const safeArea = {
    get() {
        return {
            statusBarHeight: STATUS_BAR_HEIGHT / pr,
            titleBarHeight: titleBarHeight / pr,
        };
    },
};
export const { statusBarHeight } = safeArea.get();
export function setSlideBack(enabled = true, rootTag) {
    var _a, _b;
    (_b = (_a = NativeModules.KRNBasic) === null || _a === void 0 ? void 0 : _a.setSlideBack) === null || _b === void 0 ? void 0 : _b.call(_a, {
        // @ts-ignore
        enabled,
        rootTag,
    });
}
