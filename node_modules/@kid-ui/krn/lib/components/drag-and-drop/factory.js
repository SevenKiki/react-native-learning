import Draggable from './Draggable';
import React from 'react';
import { DropAreaContext } from './context';
import { DropArea } from './DropArea';
export const createDraggableElement = (comp, key, props) => {
    return (<DropAreaContext.Provider value={key}>
      <Draggable {...props}>{comp}</Draggable>
    </DropAreaContext.Provider>);
};
export const createDropElement = (comp, key, props) => {
    return (<DropAreaContext.Provider value={key}>
      <DropArea {...props}>{comp}</DropArea>
    </DropAreaContext.Provider>);
};
