import { useRef } from 'react';
import { Animated } from 'react-native';
export default function useAnimatedValue(initialValue) {
    const lazyRef = useRef(new Animated.Value(initialValue));
    return lazyRef.current;
}
