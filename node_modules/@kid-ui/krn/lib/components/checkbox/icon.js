var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { useMemo } from 'react';
import { Svg, Path, G } from './svg';
const CheckboxIcon = (props) => {
    const { color, size, backgroundColor = 'transparent', style: propStyle } = props, rest = __rest(props, ["color", "size", "backgroundColor", "style"]);
    const style = useMemo(() => ({
        color,
        backgroundColor,
    }), [color, backgroundColor]);
    return (<Svg style={[style, propStyle]} width={size} viewBox="4 4 12 12" height={size} {...rest}>
            <G>
              <Path fill={style.color} d="m15.63 8.219-6.305 6.305-4.478-4.478L6.26 8.63l3.064 3.064 4.89-4.89 1.414 1.414Z"/>
            </G>
        </Svg>);
};
export default CheckboxIcon;
