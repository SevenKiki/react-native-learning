import type { invoke } from './invoke';
import type { addListener, removeListener } from './event';
interface Container {
    invoke: typeof invoke;
    addListener: typeof addListener;
    removeListener: typeof removeListener;
}
export declare const getContainer: () => Container;
export declare const setContainer: (container: Container) => void;
export {};
