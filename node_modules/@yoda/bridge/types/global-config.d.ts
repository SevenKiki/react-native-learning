import type { BridgeError } from './bridge-error';
import { type Mode } from './mode';
import type { YodaBridges } from './types';
export interface GlobalConfig {
    mode: Mode;
    timeout?: number;
    errorHandler?: (err: BridgeError<keyof YodaBridges>) => void;
}
export declare const globalConfig: GlobalConfig;
export declare const setGlobalConfig: (nextGlobalConfig: Partial<GlobalConfig>) => void;
