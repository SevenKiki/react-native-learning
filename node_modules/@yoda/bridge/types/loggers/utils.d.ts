import type { LoggerMethod, Logger } from './types';
export declare const createLogger: (implFor: (method: LoggerMethod) => (...data: unknown[]) => void) => Logger;
export declare const print: (context: string, method: LoggerMethod, ...data: unknown[]) => void;
