/**
 * 获取内部调用performance传递id
 * @returns id 字符串
 */
export declare function getPerformanceId(): string;
/**
 * 判断是否是performance的id
 * @param idStr 当前掉用id
 * @returns
 */
export declare function isPerformanceId(callbackId: string): boolean;
