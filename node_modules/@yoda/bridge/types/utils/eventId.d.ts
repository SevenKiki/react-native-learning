/**
 * 获取内部调用Event传递id
 * @returns id 字符串
 */
export declare function getEventId(dicData?: any): string;
/**
 * 获取纯数字的id
 * @param idStr event id 字符串
 * @returns 纯数字字符
 */
export declare function getEventIdOnlyNumber(idStr: string): string;
/**
 * 获取添加了事件前缀的id
 * @param idNumber number id
 * @returns 完整id
 */
export declare function getEventIdByNumber(idNumber: number): string;
/**
 * 判断是否是Event的id
 * @param idStr 当前掉用id
 * @returns
 */
export declare function isEventId(callbackId: string): boolean;
