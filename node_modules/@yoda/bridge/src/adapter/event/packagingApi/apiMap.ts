import { stopMonitorShake, initShakeHandler } from './shake';
import { SHAKE_EVENT } from './shake/constants';

export const yodaCustomEvent = {
    shaked: SHAKE_EVENT,
};
export const eventToInitMap = {
    [yodaCustomEvent.shaked]: initShakeHandler,
};

export const handleEventUnregisterMap = {
    [yodaCustomEvent.shaked]: stopMonitorShake,
};
